import { peeringInfoSchema } from "./peering-info-data"

// Peering request data is the same as peering info data right now
// eslint-disable-next-line unicorn/prefer-export-from
export const peeringRequestSchema = peeringInfoSchema
