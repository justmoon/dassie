/**
 * Threshold before we consider ourselves to be registered.
 */
export const TARGET_REGISTRATION_THRESHOLD = 4 / 5

/**
 * Threshold for considering another node to be a majority node.
 *
 * Number of bootstrap nodes that list the node must be greater than this number.
 */
export const MAJORITY_THRESHOLD = 1 / 2
